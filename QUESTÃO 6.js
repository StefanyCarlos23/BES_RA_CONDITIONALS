let E = [];
let O = [];

while (true) {
    let number = Number(prompt("Enter a integer (or type '000' to exit"))
    if (number === 000) {
        break
    } else if (number %2 == 0) {
        
    }
}
